#Setup
On Node1:

lrwxrwxrwx  1 hdu  hdu         19 Oct  3 07:00 elasticsearch -> elasticsearch-9.0.0
drwxr-xr-x 10 hdu  hdu       4096 Oct  3 16:05 elasticsearch-9.0.0

--start elasticsearch
--provided .bashrc is setup with 

export ES_JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64/
export PATH=$PATH:$ES_JAVA_HOME/bin
export ELASTICSEARCH_HOME=/home/hdu/elasticsearch
export PATH=$PATH:$ELASTICSEARCH_HOME/bin
export KIBANA_HOME=/home/hdu/kibana
export PATH=$PATH:$KIBANA_HOME/bin

> elasticsearch
<shows>
Elasticsearch security features have been automatically configured!
Authentication is enabled and cluster connections are encrypted.

Password for the elastic user (reset with `bin/elasticsearch-reset-password -u elastic`):
  peRqb9fiE24NZbe*dIRJ

HTTP CA certificate SHA-256 fingerprint:
  

Configure Kibana to use this cluster:
• Run Kibana and click the configuration link in the terminal when Kibana starts.
• Copy the following enrollment token and paste it into Kibana in your browser (valid for the next 30 minutes):
  

Configure other nodes to join this cluster:
• On this node:
  ⁃ Create an enrollment token with `bin/elasticsearch-create-enrollment-token -s node`.
  ⁃ Uncomment the transport.host setting at the end of config/elasticsearch.yml.
  ⁃ Restart Elasticsearch.
• On other nodes:
  ⁃ Start Elasticsearch with `bin/elasticsearch --enrollment-token <token>`, using the enrollment token that you generated

----------------------------
then after starting elasticsearch, these properties are added to elasticsearch.yml
# Enable security features
xpack.security.enabled: true
xpack.security.enrollment.enabled: true

# Enable encryption for HTTP API client connections, such as Kibana, Logstash, and Agents
xpack.security.http.ssl:
  enabled: true
  keystore.path: certs/http.p12

# Enable encryption and mutual authentication between cluster nodes
xpack.security.transport.ssl:
  enabled: true
  verification_mode: certificate
  keystore.path: certs/transport.p12
  truststore.path: certs/transport.p12
----------------------------------
--on another node (Testing)
hdu@es1:~$ curl -u elastic:peRqb9fiE24NZbe*dIRJ -XGET "https://localhost:9200/_cat/nodes?v" -k
Here -k to allow insecure connection i.e. without cert

OR
hdu@es1:~$ curl --cacert elasticsearch/config/certs/http_ca.crt https://localhost:9200/_cat/nodes?v -u elastic:peRqb9fiE24NZbe*dIRJ

adding nodes
on Node1:
--Generate token
elasticsearch-create-enrollment-token -s node


--start other nodes
--cluster-configs2 contains configs to start another node using a different port
ES_PATH_CONF=cluster-configs-2 elasticsearch --enrollment-token eyJ2ZXIiOiI4LjE0LjAiLCJh...

--cluster-configs3 contains configs to start another node using a different port
ES_PATH_CONF=cluster-configs-3 elasticsearch --enrollment-token eyJ2ZXIiOiI4LjE0LjAiLCJhZH...

--check again
hdu@es1:~$ curl -u elastic:peRqb9fiE24NZbe*dIRJ -XGET "https://localhost:9200/_cat/nodes?v" -k

---------------------------
Using setup scripts
--make sure configs dont have security related settings enabled or
if enabled then they are correct

run
./setup3.sh
./setup4.sh
elasticsearch-reset-password -u elastic
<Y>
Password for the [elastic] user successfully reset.
New value: XHZKMQbjOWvmWRbSvXk5
now using the pswd:
curl -u elastic:XHZKMQbjOWvmWRbSvXk5 -XGET "https://localhost:9200/_cat/nodes?v" -k

For kibana
edit kibana/config/ibana.yml

server.port: 5601
server.host: "10.156.0.15" --or ipaddress of your machine
server.name: "mykibana"
elasticsearch.hosts: 
  - http://localhost:9200

--start kibana
> kibana

--use the provided the url in browser and replace the ip with external ip

--now from terminal
--generate token for kibana
elasticsearch-create-enrollment-token -s kibana

--paste this token in kibana 
--let the setup complete

--use 
username: elastic
pswd: as generated above..      






